<%= form_with(model: hobby, local: true) do |form| %>
  <% if hobby.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hobby.errors.count, "error") %> prohibited this hobby from being saved:</h2>

      <ul>
      <% hobby.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<%= form.label :description %>
<%= form.text_field :description %>
<%= form.label :person_id %>
<%= form.number_field :person_id %>
<div class="field">
  <%= form.label :confirm %>
  <%= form.check_box :confirm %>

</div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
